# 旅遊業者網站開發提案

**提案日期：** 2026-06-09
**提案對象：** 找到了旅行社（小組）
**提案人：** MatsuiLin101

---

## 一、專案概述

本提案為貴小組規劃一套獨立的旅遊業者網站，採用現代化技術架構開發，版面設計與現有網站明顯區隔，並提供前台瀏覽、行程查詢、線上諮詢與後台內容管理功能。

**行程內容管理方式**經討論後，客戶已選定 **方案 A · 多檔上傳** 作為本次開發與交付範圍。此方案操作成本最低，適合既有 PDF 或圖片型行程資料快速上架；方案 B 與方案 C 的文字仍保留於提案中，作為後續若需擴充 Rich Text 編輯器、強化文字 SEO 或進行內容轉型時的參考。

| 方案 | 狀態 | 說明 | 開發時程 | 報價 |
|------|------|------|----------|------|
| A · 多檔上傳 | 本次選定 | 同時上傳多個 PDF 或圖片檔，網頁以連續圖片方式顯示 | 2 週 | NT$45,000 |
| B · Rich Text 編輯器 | 保留為後續擴充選項 | 行程內容在後台 CMS 直接編輯（逐日行程、費用含不含），完整自訂樣式 | 2.5 週 | NT$45,000 |
| C · 兩者皆有 | 保留為後續擴充選項 | 同時支援多檔上傳與 Rich Text 編輯，可混用或逐步遷移 | 3 週 | NT$50,000 |

---

## 二、功能規格

### 導覽列
1. 目的地分類選單（兩層：大區 → 小區）
2. 行程關鍵字搜尋（含目的地分類篩選、即時搜尋建議）
3. 動態麵包屑導覽
4. 社群媒體連結（Facebook、Instagram、LINE；由後台設定，留空則不顯示）

### 首頁
5. 首頁輪播大圖（每隔數秒自動切換目的地圖片，含左右切換箭頭）
6. 目的地大分類格狀瀏覽（含縮圖、路線數量）

### 行程相關
7. 目的地小分類格狀瀏覽（含縮圖、路線數量）
8. 行程列表頁（含小分類頁籤切換、tag 標籤、行程價格）
9. 行程詳情（Modal）：行程圖片 / 內容、名稱、tag、價格
10. LINE 諮詢按鈕（導向後台設定的 LINE 連結；未設定時停用）
11. 線上諮詢表單（Modal：姓名、行動電話、Email、LINE ID、諮詢內容）
12. 行程分享按鈕（複製每支行程的 `/tours/[slug]` 永久 URL，並顯示已複製提示）

### 後台系統
13. 帳號登入（管理員 / 一般帳號兩種權限，以帳號名稱登入）
14. 同時間只允許一位使用者登入後台（含心跳機制自動清除逾時 Session）
15. 行程管理（新增 / 編輯 / 刪除）
    - 基本欄位（三方案共用）：行程名稱、目的地分類、標籤、行程價格、縮圖、SEO 標題、SEO 描述、OG 圖片、發布狀態
    - 方案 A 額外欄位：行程檔案上傳（可同時上傳多個 PDF 或圖片檔，支援 10–20 MB 級單檔、個別刪除與拖曳排序）
    - 方案 B 額外欄位：Rich Text 編輯器（逐日行程、費用含不含）
    - 方案 C 額外欄位：行程檔案上傳 + Rich Text 編輯器（兩者並存）
16. 行程批次操作（於列表頁多選後執行）
    - 批次刪除
    - 批次發布 / 取消發布
    - 批次更改目的地（大區 / 小區）
    - 批次修改標籤
17. 行程拖曳排序（列表頁可直接拖曳調整顯示順序）
18. 目的地管理（大區 / 小區 CRUD、縮圖、SEO 欄位、拖曳排序）
19. 標籤管理（新增 / 編輯 / 刪除 / 拖曳排序）
20. 首頁輪播大圖管理（新增 / 編輯 / 刪除 / 拖曳排序，與前台即時同步）
21. 線上諮詢 Email 通知（表單送出後以 Gmail SMTP 寄給管理員本人，不於後台留存諮詢記錄）
22. 操作日誌（記錄後台操作，含操作人、資源、時間；限管理員查看）
23. 社群連結管理（Facebook、Instagram、LINE 網址設定；留空則前台隱藏該按鈕）
24. 帳號管理（管理員可新增 / 編輯 / 刪除帳號、重設密碼；一般帳號僅可管理行程與目的地）
25. 後台行程預覽（在後台模擬前台顯示效果，含桌機 / 手機切換）

### 其他
26. 頁尾公司資訊（執照、統編）
27. Google Tag Manager（可自行在 GTM 後台加掛 GA4 或其他追蹤碼）
28. 技術 SEO（Meta tags、Sitemap、robots.txt；每頁、每支行程均可設定 SEO 欄位）
29. SSL 憑證（Cloudflare 配發，自動免費）

> **備註：** 本報價範圍含以上列出之功能。若需新增未列於本提案之功能、頁面或第三方整合，將另行報價。

---

## 三、頁面架構

### 全站共用元素
- **導覽列**：Logo、目的地分類選單（兩層）、行程搜尋（含分類篩選）、社群媒體連結（後台設定）、麵包屑
- **頁尾**：公司資訊（執照、統編）

### 前台頁面

```
首頁
├── 輪播大圖（後台管理，每隔數秒自動切換）
└── 目的地大分類格狀瀏覽

目的地小分類頁（點選大分類後顯示）
└── 小分類格狀瀏覽（如日本 → 北海道、東京、關西…）

行程列表頁（點選小分類後顯示）
├── 小分類頁籤（同頁切換，不重新載入）
└── 行程卡片列表（含 tag、行程價格）

行程詳情（Modal，不離開當前頁面）
├── 行程內容（依方案：圖片 / Rich Text / 兩者）
├── 行程名稱、tag、價格
├── LINE 諮詢按鈕
├── 線上諮詢表單（按鈕開啟子 Modal）
└── 行程分享按鈕（複製永久 URL，顯示已複製提示）

行程詳情永久頁（/tours/[slug]，行程 URL 分享後直接開啟）
├── 與 Modal 相同內容
└── 完整 Open Graph / SEO metadata

線上諮詢表單（子 Modal）
├── 聯絡人、行動電話（必填）
├── Email、LINE ID（選填）
├── 諮詢內容（必填）
└── 送出 → 顯示確認訊息 + Email 通知管理員
```

### 後台頁面

```
後台登入頁

後台儀表板（導覽入口）

目的地管理
├── 大區列表（可拖曳排序）
├── 大區新增 / 編輯（縮圖、SEO 欄位）
├── 小區列表（可拖曳排序）
└── 小區新增 / 編輯（縮圖、SEO 欄位）

標籤管理（列表、新增、編輯、拖曳排序）

行程管理
├── 行程列表（篩選、批次操作、拖曳排序）
├── 行程新增 / 編輯（依方案管理行程內容、SEO 欄位、發布狀態）
└── 行程預覽（桌機 / 手機模擬）

首頁輪播管理（列表、新增、編輯、拖曳排序）

社群連結設定（Facebook、Instagram、LINE 網址）

操作日誌（僅管理員）

帳號管理（僅管理員）
├── 帳號列表
├── 帳號新增 / 編輯
└── 重設密碼
```

### 頁面數量總覽

| 類別 | 頁面 | 數量 |
|------|------|------|
| 前台 | 首頁、目的地小分類頁、行程列表頁、行程詳情永久頁 | 4 頁 |
| 前台 Modal | 行程詳情、線上諮詢表單 | 2 個 |
| 後台 | 登入頁、儀表板、目的地、標籤、行程、輪播管理、社群連結、操作日誌、帳號管理 | 9 頁 |
| **合計** | | **13 頁 + 2 Modal** |

> Modal 不跳轉頁面，在當前頁面覆蓋開啟。

---

## 四、設計方向

設計方向經討論後，客戶選擇以 **設計方案 C · 數字主張** 作為基礎，再調整為目前的版面。現行設計保留設計方案 C 強調資訊層次、專業信賴感與清楚分類的方向，同時降低原本深色數據化視覺的厚重感，改採首頁輪播大圖、毛玻璃效果導覽列與較輕盈的色彩配置。

目前版面配色以珊瑚玫瑰紅（`#E0506B`）與薰衣草紫（`#7166D6`）為主，搭配輕盈漸層背景貫穿全站，讓旅遊圖片、目的地分類與行程卡片成為主要視覺重點。

互動版預覽請以瀏覽器開啟 `design/github/demo.html`。

### 原設計風格選項

| 版本 | 風格名稱 | 首頁預覽 | 行程詳情預覽 |
|------|----------|----------|--------------|
| A | 編輯雜誌風 | `designs/Discovered_index_files/v2-a-magazine.html` | `designs/Discovered_tour_files/tour-a-magazine.html` |
| B | 側邊直書 | `designs/Discovered_index_files/v2-b-vertical.html` | `designs/Discovered_tour_files/tour-b-vertical.html` |
| C | 數字主張（本次選定的設計基礎） | `designs/Discovered_index_files/v2-c-numbers.html` | `designs/Discovered_tour_files/tour-c-numbers.html` |
| D | Lookbook | `designs/Discovered_index_files/v2-d-lookbook.html` | `designs/Discovered_tour_files/tour-d-lookbook.html` |

### 風格說明

**A · 編輯雜誌風**
- 報紙分欄排版，文字與圖片並重
- 深酒紅配色，質感典雅
- 行程詳情呈現：詩意標題、多圖橫排、逐日行程列表、費用含不含清單

**B · 側邊直書**
- 全版大圖 Hero，側邊直書文字裝飾
- 日系侘寂美學，暗色系為主
- 行程詳情呈現：大圖開場、清晰的 N 日行程表、底部黑底報價欄

**C · 數字主張**
- 用數字說話（國家數、路線數、方案數）
- 深色系，專業信賴感強
- 行程詳情呈現：大數字統計欄、全寬 Hero、數據化行程表
- 本次採用此方向作為基礎，並調整為更明亮、輕盈、適合旅遊圖片展示的現行版面

**D · Lookbook**
- 全螢幕沉浸式，目的地以大圖依序滾動呈現
- 極簡奢華，圖片主導
- 行程詳情呈現：全版圖片疊字，沉浸式閱讀體驗

> **關於行程詳情頁的設計說明：** 設計稿中的逐日行程、費用含不含等文字排版以方案 B / C 為基準。選擇方案 A 時，相同的頁面框架與風格不變，行程內容區域改以上傳的 PDF 或圖片檔呈現。

---

## 五、技術規格

| 項目 | 技術選型 |
|------|----------|
| 前台框架 | Next.js 16（React 19）— 支援 SSR，對 SEO 友好 |
| 後台 API | Next.js API Routes — 與前台共用同一專案 |
| 資料庫 | PostgreSQL（與主機同環境部署，或依需求改用託管資料庫） |
| ORM / 資料模型 | Prisma |
| 行程內容（方案 A） | 多檔上傳（PDF 或圖片），可同時選取多個檔案，統一儲存至 Cloudflare R2 |
| 行程內容（方案 B） | Tiptap Rich Text 編輯器，內容存於資料庫 |
| 行程內容（方案 C） | 同時支援多檔上傳與 Rich Text，後台可混用 |
| 行程檔案 / 圖片儲存 | Cloudflare R2（S3-compatible Object Storage） |
| 大檔案上傳方式 | 前端取得授權後直接上傳至 Cloudflare R2，避免應用程式伺服器轉傳大型檔案 |
| 清單排序 | dnd-kit（拖曳排序，適用後台清單） |
| 後台單一登入 | JWT + Server-side Session（登入時記錄，登出或心跳中斷時清除） |
| 帳號權限 | 管理員：帳號管理、操作日誌、所有 CRUD；一般帳號：行程、目的地管理 |
| 社群連結設定 | SiteSetting 單筆設定（Facebook、Instagram、LINE），前台 Header / Footer / LINE 諮詢按鈕即時讀取；留空則隱藏或停用 |
| 線上諮詢表單 | 表單送出後即時以 Gmail SMTP（nodemailer）寄送 Email 通知管理員；不於資料庫留存諮詢單 |
| 操作日誌 | 後台 CUD 操作自動記錄至 ActivityLog，含操作人與資源名稱 |
| Analytics | Google Tag Manager — 在 GTM 後台掛載 GA4 或其他追蹤碼，無需改程式碼 |

### 行程內容方案比較

| | 方案 A · 多檔上傳 | 方案 B · Rich Text | 方案 C · 兩者皆有 |
|---|---|---|---|
| 行程維護方式 | 直接上傳 PDF 或圖片（可多檔同時上傳） | 後台 CMS 編輯 | 兩者均可 |
| 手機體驗 | 好 | 好 | 好 |
| SEO | 圖片 / PDF 內文較不易被完整收錄 | 完整 | 完整 |
| 學習成本 | 低 | 中（需學後台編輯器） | 中 |
| 彈性 | 中（版面固定於上傳檔案） | 高 | 最高 |

### 技術調整說明

- **行程檔案儲存**：客戶行程檔案單檔可能介於 10–20 MB，已不適合採 Cloudinary 免費方案；改採 Cloudflare R2，可支援較大的 PDF / 圖片檔案，且出站流量免費，較適合行程檔案瀏覽情境。
- **大檔案上傳**：行程檔案不經由應用程式伺服器轉傳；後台由系統產生上傳授權，檔案直接上傳至 R2，完成後只將檔案 URL、檔名、排序等 metadata 存入資料庫。
- **Email 通知**：依客戶需求，採 Gmail SMTP（nodemailer）由指定 Gmail 帳號寄送諮詢通知給管理員本人；此用途適合低量表單通知，不作大量行銷信或大量交易信使用。
- **社群連結設定**：Facebook、Instagram、LINE 連結集中於後台社群連結頁維護，前台 Header / Footer 及 LINE 諮詢按鈕會讀取同一份設定；設定異動會寫入操作日誌。
- **Analytics 管理**：採 Google Tag Manager，後續可自行掛載 GA4、廣告追蹤碼或其他事件追蹤。
- **後台登入**：採帳號名稱登入，適合內部人員管理，不強制每個帳號綁定 Email。

---

## 六、部署方案

考量本案為商業網站，且行程檔案單檔可能達 10–20 MB；同時部署與維護可由提案人處理，因此建議採 **AWS Lightsail 經濟部署方案**。此方案以固定月費主機承載 Next.js 與 PostgreSQL，行程檔案則獨立存放於 Cloudflare R2，兼顧成本、檔案容量與正式營運需求。

| 項目 | 建議服務 | 費用說明 |
|------|----------|----------|
| 網域 | Cloudflare Registrar | 實報實銷（依實際購買網域而定） |
| DNS / SSL / 基礎 CDN | Cloudflare | 免費方案即可 |
| 主機 / 後端 | AWS Lightsail Linux Instance | 約 US$5–12 / 月，依 CPU / RAM 規格選擇 |
| 資料庫 | PostgreSQL（同 Lightsail 主機自管） | 無額外月費；資料庫容量受主機磁碟空間限制 |
| 行程檔案 / 圖片儲存 | Cloudflare R2 | 10 GB-month 免費；超出後約 US$0.015 / GB-month，出站流量免費 |
| 諮詢表單 Email | Gmail SMTP（App Password） | 使用客戶指定 Gmail 帳號寄送給管理員本人，無額外服務月費 |
| Analytics | Google Tag Manager + GA4 | 免費 |
| **固定月費預估** | | **約 US$5–12 / 月起，不含網域、備份快照與 R2 超量費用** |

> **備註：** 第三方服務費由貴方帳號自行支付，實際金額依服務商當期定價、用量與匯率為準。

### 部署架構

```
Cloudflare DNS / SSL
└── AWS Lightsail
    ├── Nginx 反向代理
    ├── Next.js App（前台 + 後台 + API）
    └── PostgreSQL（行程資料、分類、標籤、網站設定、操作日誌）

Cloudflare R2
└── 行程 PDF / 圖片檔案、縮圖、OG 圖片

Gmail SMTP
└── 諮詢表單 Email 通知
```

### 備份與維護

| 項目 | 作法 |
|------|------|
| 程式部署 | Git pull / build / restart，或以 Docker Compose 部署 |
| SSL | Cloudflare SSL + Nginx origin 設定 |
| 資料庫備份 | 定期 `pg_dump`，備份檔可存至 Lightsail 磁碟或 Cloudflare R2 |
| 主機備份 | 視需求啟用 Lightsail Snapshot |
| 系統維護 | 定期更新套件、安全性修補、監控磁碟與記憶體 |
| 還原方式 | 使用資料庫備份 + 程式碼版本 + R2 檔案重建服務 |

### GCP 備選方案

若偏好使用 GCP，也可改採 **Google Cloud Run + Neon PostgreSQL + Cloudflare R2**。此方案維運負擔低於 VPS，但費用較受流量、Cloud Run 使用量與外部資料庫用量影響；若主要目標是最低固定月費，AWS Lightsail 較適合。

### 行程檔案容量估算

以 Cloudflare R2 儲存行程檔案，若單一檔案平均為 10–20 MB，可先用以下方式估算容量：

| R2 儲存量 | 約可儲存 10 MB 檔案 | 約可儲存 20 MB 檔案 |
|-----------|---------------------|---------------------|
| 10 GB（免費額度內） | 約 1,000 個 | 約 500 個 |
| 50 GB | 約 5,000 個 | 約 2,500 個 |
| 100 GB | 約 10,000 個 | 約 5,000 個 |

R2 出站流量免費，較適合行程 PDF / 圖片常被前台瀏覽的情境；仍需監控讀取請求數、儲存量與檔案大小，避免大量未壓縮檔案造成後台操作與前台載入速度下降。

### 使用量監控通知

各服務均設定使用量達 **80%** 時自動發送 Email 通知：

| 服務 | 監控項目 |
|------|----------|
| AWS Lightsail | CPU、記憶體、磁碟空間、流量 |
| PostgreSQL | 資料庫容量、連線數、備份狀態 |
| Cloudflare R2 | 儲存空間、請求數 |
| Cloudflare | 流量異常、錯誤率 |
| Gmail SMTP | 寄信量、寄送錯誤、帳號驗證狀態 |

---

## 七、開發時程

配合 AI 輔助開發工具，本次已選定 **方案 A · 多檔上傳**，開發時程預估 2 週。方案 B 與方案 C 的時程保留如下，作為後續擴充 Rich Text 編輯器或雙模式內容管理時的參考。

**方案 A · 多檔上傳（2 週）**

| 週次 | 工作內容 |
|------|----------|
| 第 1 週 | 專案建置、資料庫設計、後台登入系統（含單一登入、角色權限）、行程管理 CRUD + R2 直傳多檔上傳、目的地與標籤管理 |
| 第 2 週 | 前台頁面開發（首頁 + 輪播 + 列表 + Modal + 諮詢表單）、GTM 整合、SEO、操作日誌、整合測試、修 bug、部署上線 |

**方案 B · Rich Text 編輯器（2.5 週）**

| 週次 | 工作內容 |
|------|----------|
| 第 1 週 | 專案建置、資料庫設計、後台登入系統（含單一登入、角色權限）、目的地與標籤管理 |
| 第 2 週 | 後台行程管理 CRUD + Rich Text 編輯器整合、SEO 欄位、發布狀態與預覽 |
| 第 3 週（前半） | 前台頁面開發、GTM 整合、整合測試、部署上線 |

**方案 C · 兩者皆有（3 週）**

| 週次 | 工作內容 |
|------|----------|
| 第 1 週 | 專案建置、資料庫設計、後台登入系統（含單一登入、角色權限）、目的地與標籤管理 |
| 第 2 週 | 後台行程管理 CRUD + R2 直傳多檔上傳 + Rich Text 編輯器、批次操作與拖曳排序 |
| 第 3 週 | 前台頁面開發、GTM 整合、SEO、操作日誌、整合測試、修 bug、部署上線 |

---

## 八、報價

### 開發費用

本次客戶已選定 **方案 A · 多檔上傳**，網站開發費為 **NT$45,000**。方案 B 與方案 C 保留於下表，作為後續功能擴充或內容管理方式調整時的估價基準。

| 方案 | 狀態 | 行程內容管理方式 | 開發時程 | 網站開發費 |
|------|------|-----------------|----------|------------|
| A · 多檔上傳 | 本次選定 | 同時上傳多個 PDF 或圖片檔，網頁以連續圖片方式顯示 | 2 週 | NT$45,000 |
| B · Rich Text 編輯器 | 保留為後續擴充選項 | 後台 CMS 直接編輯行程內容，完整自訂版面 | 2.5 週 | NT$45,000 |
| C · 兩者皆有 | 保留為後續擴充選項 | 多檔上傳與 Rich Text 並存，可混用或逐步遷移 | 3 週 | NT$50,000 |

| 項目 | 費用 |
|------|------|
| 年度網域費 | 預估 NT$280–500 / 年，實報實銷（依實際購買網域而定） |

### 本次基礎交付已納入

以下功能已納入本次基礎交付範圍，不另行加價：

- 首頁輪播大圖後台管理
- 支援 10–20 MB 級行程檔案的 R2 直傳上傳流程
- 社群連結後台設定（Facebook、Instagram、LINE）
- 全站後台拖曳排序
- 行程批次操作（批次刪除 / 發布 / 更改目的地 / 修改標籤）
- 線上諮詢表單 Email 通知
- 操作日誌
- 行程永久頁（支援 SEO、Open Graph metadata 與複製分享連結）
- 後台行程預覽（桌機 / 手機）

### 售後服務（開發完成後 1 年內，免費）

| 項目 | 說明 |
|------|------|
| Bug 修復 | 功能異常、顯示錯誤等問題排查與修復 |
| 固定內容調整 | 非後台管理的內容修改，如頁尾公司資訊、聯絡電話、執照字號等 |
| 服務異常排查 | 主機、資料庫、第三方服務異常協助處理 |
| 使用諮詢 | 後台操作問題解答 |

### 以下需另行報價

- 新增功能或頁面
- 設計風格大幅變更或重新設計
- 從方案 A 或 B 擴充至方案 C
- 第三方系統串接（如金流、CRM、ERP、簡訊服務等）
- 1 年免費期結束後的持續維護服務

### 報價說明

- 本報價為優惠價，市場同規格報價通常為 NT$80,000 以上
- 本次交付範圍以方案 A 為準；方案 B 與方案 C 為後續擴充參考
- 年度網域費實報實銷（預估 NT$280–500）；主機、備份快照與行程檔案儲存服務費由貴方依實際用量自行支付
- 本報價範圍含第二節所列功能，**額外需求另行報價**
